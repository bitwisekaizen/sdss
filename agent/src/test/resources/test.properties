# Randomly generated for test
app.server.port=0

app.persistence.jdbcUrl=jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS test
spring.jpa.generate-ddl=false
spring.jpa.hibernate.ddl-auto=create-drop

app.storage.network.addresses=1.1.1.1,2.2.2.2

app.chef.executable=cat
app.chef.default.args=%s
app.chef.tar.archive.url=<replace-me>